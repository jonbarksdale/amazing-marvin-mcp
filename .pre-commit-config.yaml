# ABOUTME: Pre-commit hook configuration for fast local checks.
# ABOUTME: Runs ruff linting and formatting only; heavier checks run in CI.

repos:
  - repo: https://github.com/astral-sh/ruff-pre-commit
    rev: v0.8.6
    hooks:
      - id: ruff
        args: [--fix]
      - id: ruff-format
